#!/bin/bash
. "$(dirname "$0")"/artifactory_setup
docker-compose build || exit 1
docker-compose run --rm sidekiq-publisher-console in_all_rubies.sh bin/setup \
  && echo "Setup complete! Run 'docker/start' to start the console container"
